# Artefatos de inferência

Esta pasta já contém os artefatos versionados no repositório:

- `ret_scaler.pkl` — scaler dos log-retornos
- `inference_meta.pkl` — símbolo, janela e alvo do modelo
- `metrics.pkl` — métricas de avaliação e melhores hiperparâmetros
- `AAPL_clean.csv` — usado somente nos exemplos e no teste de carga

Os notebooks 01 e 02 regeram estes arquivos. O nome do CSV muda
conforme o símbolo escolhido.
